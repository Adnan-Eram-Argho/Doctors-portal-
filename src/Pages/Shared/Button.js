import React from 'react';

const Button = ({ children }) => {
    return (
        <button class="btn  uppercase text-white text-bold bg-gradient-to-r from-secondary to-primary ...">{children}</button>
    );
};

export default Button;